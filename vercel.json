{
  "buildCommand": "prisma generate && next build && if [ \"$VERCEL_ENV\" = \"production\" ]; then prisma migrate deploy; fi",
  "installCommand": "npm install",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "preview": false
    }
  },
  "github": {
    "silent": true
  },
  "crons": [
    {
      "path": "/api/internal/cron/world-tick-sweep",
      "schedule": "0 6 * * *"
    }
  ]
}
